Flask>=3.0.0
Flask-Login>=0.6.3
Flask-WTF>=1.2.1
Flask-SQLAlchemy>=3.1.1
Flask-Limiter>=3.5.0
Flask-Migrate>=4.0.5
Pillow>=10.2.0
qrcode[pil]>=7.4.2
Werkzeug>=3.0.0
itsdangerous>=2.1.2
tzdata>=2024.1

# Production WSGI server
gunicorn>=22.0.0

# PostgreSQL driver (optional — falls back to SQLite)
psycopg2-binary>=2.9.9

# Redis client (optional — falls back to in-memory)
redis>=5.0.1

# Celery task queue (optional — falls back to threading daemon)
celery>=5.3.6

# S3-compatible object storage (optional — falls back to local filesystem)
boto3>=1.34.0

# Prometheus metrics (optional)
prometheus-flask-exporter>=0.23.0

